# Backlog по тарифам

Этот список хранит тарифные источники и куски логики, которые не закрываем в текущем проходе, но должны доделать перед более точной версией калькулятора.

| ID | Блок | Сейчас в MVP | Что нужно доделать | Приоритет |
|---|---|---|---|---|
| T-001 | Wildberries: коэффициенты складов FBO/FBS | Закрыто: `scripts/import_wb_tariffs.mjs` забирает официальные тарифы WB API по коробам, паллетам, приемке и комиссиям. | Следить за изменениями схемы API и лимитами запросов; при необходимости добавить планировщик регулярного обновления. | Закрыто |
| T-002 | Wildberries: базовая логистика, индексы и хранение | Закрыто для текущей версии: ставки WB берутся из API на дату расчета, тип поставки выбирается в интерфейсе, индекс локализации и индекс распределения продаж вводятся вручную. | Автоматизировать получение индексов, если WB откроет их в API или появится надежный кабинетный экспорт. | Средний |
| T-003 | Ozon: логистика FBO/FBS, хранение FBO и доставка до ПВЗ | Используются `logistika-fbo-fbs-01052026_1777018200.xlsx`, `Наценка за нелокальную продажу.xlsx` и `Озон_Сроки_бесплатного_размещения_010626_1778767885.xlsx`; ПВЗ = `25`, приёмка FBS = `20`, хранение FBO после бесплатного срока: стандарт `2,50 руб./л/день`, КГТ `0,10 руб./л/день`. | Закрыто для текущей версии: тариф выбирается по объёму/ВГХ, цене, кластеру отправки и кластеру доставки; для КГТ используется тот же принцип и та же таблица, что для обычных товаров; наценка за нелокальную продажу считается только для FBO; хранение считается только для FBO по бесплатным дням из справочника. | Закрыто |
| T-004 | Wildberries FBS: СГТ-товары | Закрыто для текущей версии: если SKU = WB `СГТ`, WB FBS берет отдельную строку `Маркетплейс: <федеральный округ> СГТ`; для Коледино/ЦФО контрольные значения `34,50` за первый литр, `10,50` за следующий литр, коэффициент `75%`. Если СГТ-строка не найдена, расчет откатывается на обычную FBS-строку и показывает предупреждение. | Следить за изменениями WB API и при необходимости расширить проверку доступности СГТ по складам/типам поставки. | Закрыто |
| T-005 | Wildberries: быстрая сдача и время сдачи FBS | Закрыто для текущей версии: с 2026-06-18 если SKU = WB `МГТ` или `КГТ+`, галка `Быстрая сдача` уменьшает комиссию WB FBS на `1,5 п.п.`; если SKU = WB `СГТ`, скидка не применяется и в ячейке показывается предупреждение. | Добавить отдельный ввод времени сдачи WB FBS, чтобы моделировать не только сценарий `до 13 часов`, но и окно `13-18 часов` с базовой комиссией, а также возможные правила просрочки, если они понадобятся. | Средний |
| T-006 | Ozon: КГТ-товары | Закрыто для текущей версии: SKU автоматически получает категорию Ozon `КГТ`; доставка FBO/FBS для КГТ считается по той же таблице `logistika-fbo-fbs-01052026_1777018200.xlsx` по направлению и ВГХ; хранение FBO для КГТ закрыто через справочник бесплатных дней и ставку `0,10 руб./л/день`. | Следить за изменениями Ozon: если появится отдельная официальная КГТ-ветка доставки, заменить текущую бизнес-логику на источник Ozon. | Закрыто |
| T-007 | Ozon: НДС по строкам маркетплейса | В калькуляторе все Ozon-строки маркетплейса считаются `с НДС`; логистика FBO/FBS подтверждена заголовком `Тариф, рублей с НДС`. | Подтвердить по официальному источнику или отчёту Ozon НДС-режим для комиссии, наценки за нелокальную продажу, ПВЗ `25`, FBS-приёмки `20` и ставок хранения `2,50 / 0,10`. | Средний |
